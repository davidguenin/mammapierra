{% comment %}
  Ce code utilise des blocs pour afficher un contenu différent par collection.
  Tout est géré depuis l'éditeur de thème.
{% endcomment %}

{%- liquid
  assign wysiwyg_content = ''
  assign block_found = false

  comment
    On parcourt tous les blocs de la section.
  endcomment
  for block in section.blocks
    comment
      Si l'identifiant de la collection du bloc correspond à la collection actuelle,
      on stocke son contenu et on arrête la boucle.
    endcomment
    if block.settings.collection == collection.handle
      assign wysiwyg_content = block.settings.content
      assign block_found = true
      break
    endif
  endfor
-%}

{% comment %}
  On affiche la section uniquement si un bloc correspondant a été trouvé
  et que son contenu n'est pas vide.
{% endcomment %}
{%- if block_found and wysiwyg_content != blank -%}
  <div class="color-{{ section.settings.color_scheme }} gradient">
    <div class="page-width section-{{ section.id }}-padding">
      {%- if section.settings.heading != blank -%}
        <h2 class="title title-wrapper--no-top-margin inline-richtext h2 text-center">
          {{ section.settings.heading | escape }}
        </h2>
      {%- endif -%}

      <div class="rte center">
        {{ wysiwyg_content }}
      </div>
    </div>
  </div>
{%- endif -%}

{% schema %}
{
  "name": "WYSIWYG Collection",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Titre global (optionnel)",
      "default": "Informations supplémentaires"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Jeu de couleurs",
      "default": "background-1"
    }
  ],
  "blocks": [
    {
      "type": "wysiwyg_block",
      "name": "Bloc de Contenu",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Choisir la collection"
        },
        {
          "type": "richtext",
          "id": "content",
          "label": "Contenu personnalisé",
          "default": "<p>Ajoutez votre contenu personnalisé pour la collection sélectionnée ici.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Contenu Riche par Collection"
    }
  ]
}
{% endschema %}
